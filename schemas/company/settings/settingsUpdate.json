{
  "type": "object",
  "properties": {
    "updatePeriod": {
      "type": "string",
      "enum": ["monthly", "quarterly", "semi-annually", "annually"]
    },
    "allowedToChangeConnection": {
      "type": "boolean"
    }
  }
}
