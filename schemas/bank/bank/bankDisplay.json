{
  "required": ["bankId", "email", "name"],
  "type": "object",
  "properties": {
    "bankId": {
      "type": "string",
      "example": "sbin0005999",
      "description": "The bankId the bank wishes to use while logging in."
    },
    "email": {
      "type": "string",
      "format": "email",
      "example": "sbin0005999@sbi.com",
      "description": "The email associated with the bank."
    },
    "name": {
      "type": "string",
      "example": "State Bank of India",
      "description": "The name with which the bank should be represented in the product."
    },
    "logoUrl": {
      "type": "string",
      "format": "url",
      "example": "https://example.com/example-logo.png",
      "description": "The URL for your bank's logo."
    },
    "createdAt": {
      "type": "string",
      "format": "date-time",
      "description": "The date time at which the account was created."
    }
  }
}
