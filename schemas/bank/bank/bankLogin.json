{
  "type": "object",
  "properties": {
    "bankId": {
      "type": "string",
      "example": "sbin0005999",
      "description": "The bankId the bank created while registering."
    },
    "password": {
      "type": "string",
      "format": "password",
      "example": "password",
      "description": "The password the bank created while registering."
    }
  }
}
